# 生产工艺流程追溯系统 Design Guidelines

> 本文档只定义后台系统的界面设计、交互风格与页面呈现规范。  
> 不写项目背景、技术栈、数据库规则、开发任务边界、AI 编码规则，这些内容放入 `agents.md`。

---

## 1. 设计定位

### 1.1 风格名称

**Clean Industrial Admin / 清爽型工业管理后台**

### 1.2 设计关键词

- 清爽
- 克制
- 表格优先
- 弹窗表单
- 状态清晰
- 信息密度适中
- 低装饰
- 低动效
- 适合生产、质量、仓储、工单类后台系统

### 1.3 参考风格

本系统适合参考：

- **Airtable**：表格、字段、筛选、资料库管理感
- **Linear**：任务状态、清晰流程、克制高级感
- **Stripe / Vercel 控制台**：卡片、状态、空状态、细腻边框

不采用：

- 大屏炫酷风
- 重型 ERP 灰蓝风
- 官网营销风
- 过度科技感和过度阴影

---

## 2. 色彩规范

### 2.1 主色

| 用途     | 色值      | 说明                                           |
| -------- | --------- | ---------------------------------------------- |
| 主色     | `#283A50` | 深蓝，品牌主色，用于侧边栏、标题强调、核心导航 |
| 辅助蓝   | `#306188` | 主按钮、链接、选中状态                         |
| 强调金   | `#EAB321` | 少量强调，如当前步骤、重点状态、客户展示亮点   |
| 页面背景 | `#F5F7FA` | 后台页面整体背景                               |
| 卡片背景 | `#FFFFFF` | 表格、表单、弹窗主体                           |
| 边框灰   | `#E5E7EB` | 表格线、输入框边框、分割线                     |
| 正文黑   | `#1F2937` | 主要文字                                       |
| 次级灰   | `#6B7280` | 次要说明、辅助文字                             |
| 弱灰     | `#9CA3AF` | 占位符、弱提示                                 |

### 2.2 状态色

| 状态            | 色值      | 使用场景                   |
| --------------- | --------- | -------------------------- |
| 成功 / 已完成   | `#22C55E` | 已完成、合格、已启用       |
| 警告 / 部分完成 | `#F59E0B` | 部分分配、待确认、部分合格 |
| 错误 / 异常     | `#EF4444` | 不合格、异常、失败、已停用 |
| 信息 / 进行中   | `#3B82F6` | 生产中、检测中、已派工     |
| 默认 / 待处理   | `#6B7280` | 待开始、未分配、草稿       |

### 2.3 使用原则

1. 页面整体以白色、浅灰、深蓝为主。
2. 金色只用于少量强调，不作为大面积背景色。
3. 删除、作废、取消类操作必须使用红色或危险样式。
4. 状态颜色应全系统统一，不同页面不能重复发明状态色。
5. 表格内状态优先使用标签，不使用大面积色块。

---

## 3. 字体与排版

### 3.1 字体

优先使用系统无衬线字体：

```css
font-family: Inter, 'PingFang SC', 'Microsoft YaHei', 'Noto Sans SC', Arial, sans-serif;
```

### 3.2 字号

| 场景     | 字号 | 字重 |
| -------- | ---: | ---: |
| 页面标题 | 20px |  600 |
| 区块标题 | 16px |  600 |
| 表格正文 | 14px |  400 |
| 表单标签 | 14px |  500 |
| 辅助说明 | 12px |  400 |
| 状态标签 | 12px |  500 |

### 3.3 间距

| 场景                 |        间距 |
| -------------------- | ----------: |
| 页面内容边距         |        24px |
| 卡片内边距           | 16px / 20px |
| 表单项垂直间距       |        16px |
| 按钮间距             |         8px |
| 表格工具栏与表格间距 |        12px |

---

## 4. 页面整体布局

### 4.1 管理端基础布局

管理端采用固定后台结构：

```text
┌──────────────────────────────────────────────┐
│ 顶部栏：系统名称 / 面包屑 / 用户信息           │
├───────────────┬──────────────────────────────┤
│ 左侧菜单       │ 页面内容区                     │
│               │ 搜索筛选区                     │
│               │ 表格 / 分组卡片 / 详情区域       │
│               │ 分页                           │
└───────────────┴──────────────────────────────┘
```

### 4.2 左侧菜单

1. 左侧菜单宽度建议 220px。
2. 菜单最多两级，不建议三级嵌套。
3. 当前菜单使用深蓝背景或蓝色左边线高亮。
4. 菜单图标可使用线性图标，保持轻量。
5. 一级分区建议包括：系统管理、产品管理、仓储管理、生产管理、质量管理。

### 4.3 顶部栏

顶部栏包含：系统名称、当前页面面包屑、当前登录用户、退出登录。  
不在顶部栏放太多业务按钮。

---

## 5. 页面类型规范

### 5.1 列表页

适用于用户管理、角色管理、产品资料、产品分类、生产工序、工艺路线、库存管理、出入库管理、工单管理、任务管理、检验记录、返工记录。

基础结构：

```text
页面标题
说明文字，可选

搜索筛选区
[关键词] [状态] [日期] [查询] [重置]                       [新增]

表格
分页
```

设计要求：

1. 搜索区放在表格上方，使用白色卡片或轻量区域。
2. 主操作按钮放在右上角。
3. 行内操作不超过 4 个，超过后放入“更多”。
4. 表格字段优先展示业务识别信息、状态、负责人、时间、操作。
5. 详情查看可用弹窗，不用抽屉。

### 5.2 分块列表页

适用于物料分配、派工管理、生产报工、检验记录按批次展示、返工记录按批次展示。

基础结构：

```text
批次信息卡片
  工单号 / 产品 / 数量 / 当前状态

  明细表格
  操作按钮组
```

设计要求：

1. 每个生产批次作为一个独立区块。
2. 区块顶部展示批次号、工单号、产品、数量、状态。
3. 区块内表格展示物料、工序、检测、返工等明细。
4. 状态应在区块头部明显展示。
5. 分块之间留 16px 间距。

### 5.3 详情页

管理端详情不优先做独立页面。  
如需展示大量信息，使用“详情弹窗”或“详情页路由”。

详情内容建议使用 Tabs：

- 基础信息
- 工艺路线
- 物料记录
- 报工记录
- 检验记录
- 返工记录
- 成品流转
- 操作日志

---

## 6. 弹窗规范

### 6.1 总原则

本系统管理端统一使用 **弹窗 Modal** 进行新增、编辑、查看详情、分配、确认类操作。  
不使用右侧抽屉 Drawer 作为主要交互方式。

### 6.2 弹窗尺寸

| 弹窗类型 | 宽度建议 | 场景                               |
| -------- | -------: | ---------------------------------- |
| 小弹窗   |    420px | 确认、启停、重置密码               |
| 中弹窗   |    640px | 新增/编辑用户、分类、工序          |
| 大弹窗   |    860px | 产品资料、工艺路线、物料分配、报工 |
| 超大弹窗 |   1000px | 复杂详情、批次完整信息             |

### 6.3 弹窗结构

```text
弹窗标题
弹窗说明，可选

表单内容 / 表格内容 / 分组内容

底部按钮：
[取消] [确认]
```

### 6.4 弹窗按钮

1. 取消按钮在左，确认按钮在右。
2. 危险确认使用红色按钮。
3. 保存草稿、确认提交、下达工单等业务动作要使用明确文案。
4. 不使用只有“确定”的模糊按钮，尽量写成“确认分配”“保存产品”“提交报工”。

---

## 7. 表格规范

### 7.1 表格样式

1. 表头背景使用 `#F9FAFB`。
2. 表格边框使用 `#E5E7EB`。
3. 行高建议 48px。
4. 鼠标悬停行背景使用 `#F3F4F6`。
5. 重要识别字段可加粗，如工单号、批次号、产品型号。

### 7.2 表格列顺序

推荐顺序：

```text
识别字段 → 名称/型号 → 分类/类型 → 数量 → 状态 → 负责人 → 时间 → 操作
```

### 7.3 操作列

常用操作顺序：

```text
查看 → 编辑 → 业务动作 → 启停/删除
```

危险操作放入“更多”或二次确认弹窗。

---

## 8. 表单规范

### 8.1 表单布局

1. 简单表单使用单列。
2. 中等复杂表单使用两列。
3. 产品资料、工单、批次类表单可以使用分组标题。
4. 必填项使用红色星号。
5. 动态字段应放在“规格参数”分组内。

### 8.2 表单分组

推荐分组：

- 基础信息
- 规格参数
- 关联信息
- 数量信息
- 文件附件
- 备注说明

### 8.3 输入控件

| 数据类型 | 控件           |
| -------- | -------------- |
| 文本     | Input          |
| 长文本   | Textarea       |
| 状态     | Select         |
| 分类     | Select         |
| 数量     | InputNumber    |
| 日期     | DatePicker     |
| 时间     | DateTimePicker |
| 文件     | Upload         |
| 是否     | Switch         |
| 多选     | CheckboxGroup  |

---

## 9. 状态标签规范

### 9.1 工单状态

| 状态      | 文案   | 颜色 |
| --------- | ------ | ---- |
| draft     | 草稿   | 灰色 |
| released  | 已下达 | 蓝色 |
| doing     | 生产中 | 蓝色 |
| completed | 已完工 | 绿色 |
| closed    | 已关闭 | 灰色 |
| cancelled | 已取消 | 红色 |

### 9.2 物料状态

| 状态             | 文案       | 颜色 |
| ---------------- | ---------- | ---- |
| ungenerated      | 未生成需求 | 灰色 |
| unassigned       | 未分配     | 灰色 |
| partial_assigned | 部分分配   | 橙色 |
| assigned         | 已分配     | 蓝色 |
| ready            | 已齐套     | 绿色 |
| outbound         | 已出库     | 绿色 |
| shortage         | 缺料       | 红色 |
| returned         | 已退料     | 灰色 |

### 9.3 派工/报工状态

| 状态      | 文案   | 颜色 |
| --------- | ------ | ---- |
| pending   | 待处理 | 灰色 |
| assigned  | 已派工 | 蓝色 |
| doing     | 进行中 | 蓝色 |
| completed | 已完成 | 绿色 |
| abnormal  | 异常   | 红色 |
| skipped   | 已跳过 | 灰色 |

### 9.4 检验结果

| 状态         | 文案     | 颜色 |
| ------------ | -------- | ---- |
| pass         | 合格     | 绿色 |
| fail         | 不合格   | 红色 |
| partial_pass | 部分合格 | 橙色 |

---

## 10. 按模块页面设计

### 10.1 系统管理

#### 用户管理 `/system/users`

页面类型：列表页 + 弹窗表单

- 搜索：账号、姓名、部门、状态
- 表格：账号、姓名、部门、岗位、角色、状态、创建时间、操作
- 操作：查看、新增、编辑、启停、重置密码、分配角色
- 弹窗：新增/编辑用户、分配角色、重置密码确认

#### 角色管理 `/system/roles`

页面类型：列表页 + 弹窗表单

- 搜索：角色名称、角色编码、状态
- 表格：角色名称、角色编码、状态、备注、创建时间、操作
- 操作：查看、新增、编辑、删除、启停、分配权限
- 弹窗：新增/编辑角色、分配权限

#### 日志管理 `/system/logs`

页面类型：列表页

- 搜索：用户、操作类型、模块、时间范围
- 表格：时间、用户、模块、操作、对象、结果、IP/备注
- 操作：查看详情

---

### 10.2 产品管理

#### 产品资料 `/product/products`

页面类型：列表页 + 弹窗表单

- 搜索：产品型号、产品名称、分类、获取方式、状态
- 表格：产品型号、产品名称、属性、类型、获取方式、单位、状态、操作
- 操作：查看、新增、编辑、启停、查看库存、查看工艺路线
- 弹窗：新增/编辑产品、产品用料清单配置、查看库存、查看工艺路线

#### 产品分类 `/product/categories`

页面类型：列表页 + 弹窗表单

- 搜索：产品属性、产品类型、状态
- 表格：产品属性、产品类型、关联规格参数、状态、操作
- 操作：查看、新增、编辑、启停、配置规格参数
- 弹窗：新增/编辑分类、配置规格参数

#### 生产工序 `/product/processes`

页面类型：列表页 + 弹窗表单

- 搜索：工序编码、工序名称、状态
- 表格：工序编码、工序名称、工序说明、技术文件、状态、操作
- 操作：查看、新增、编辑、启停、上传工序技术文件
- 弹窗：新增/编辑工序、上传技术文件

#### 工艺路线 `/product/routes`

页面类型：列表页 + 大弹窗配置

- 搜索：路线名称、适用产品、状态
- 表格：路线名称、工序顺序、适用产品、版本、状态、操作
- 操作：查看、新增、编辑、删除、启停、配置工序顺序
- 弹窗：新增/编辑路线、配置工序顺序

---

### 10.3 仓储管理

#### 库存管理 `/warehouse/inventory`

页面类型：列表页

- 搜索：物料名称、物料批次、状态
- 表格：物料名称、物料批次、库存数量、可用数量、预留数量、状态、操作
- 操作：查看库存、查看可用库存、查看预留库存、库存盘点
- 弹窗：库存详情、库存盘点

#### 出入库管理 `/warehouse/transactions`

页面类型：列表页 + 弹窗表单

- 搜索：类型、物料名称、批次号、关联任务、时间
- 表格：类型、物料名称、批次号、数量、关联任务、时间、操作人、操作
- 操作：入库、出库、发运、退料、查看记录
- 弹窗：入库、出库、发运、退料、记录详情

---

### 10.4 生产管理

#### 工单管理 `/production/orders`

页面类型：列表页 + 弹窗表单

- 搜索：工单号、产品、状态、负责人、计划时间
- 表格：工单号、产品、数量、负责人、当前流程、状态、下一步、计划完成时间、操作
- 操作：查看、新增、编辑、保存草稿、下达工单、关闭工单、取消工单、分配生产任务
- 弹窗：新增/编辑工单、工单详情、分配生产任务

#### 任务管理 `/production/tasks`

页面类型：列表页 + 快捷动作弹窗

- 搜索：生产批次号、工单号、产品、状态、负责人
- 表格：生产批次号、工单号、产品、数量、物料状态、派工状态、生产状态、检验状态、操作
- 操作：查看、新增、编辑、生成物料需求、分配物料、派工、开始生产、完成生产、创建返工、查看追溯、跳转入库
- 弹窗：任务详情、生成物料需求、分配物料、派工、创建返工

#### 物料分配

页面类型：按生产批次分块展示

- 区块顶部：批次号、工单号、产品、数量、物料状态
- 明细表格：物料名称、需求数量、已分配、未分配、物料批次、状态
- 操作：生成物料需求、分配物料、确认齐套、确认出库、退料
- 弹窗：生成物料需求、分配物料、确认齐套、确认出库、退料

#### 派工管理

页面类型：按生产批次分块展示

- 区块顶部：批次号、工单号、产品、数量、任务状态
- 明细表格：工序、默认派工对象、派工对象、工序派工状态、操作
- 操作：一键按默认派工、清除全部派工、单工序派工、改派
- 弹窗：派工、改派、清除派工确认

#### 生产报工

页面类型：按生产批次分块展示

- 区块顶部：批次号、工单号、产品、数量、任务状态
- 明细表格：工序、操作员、开工时间、完工时间、合格/总数、报工状态、操作
- 操作：开工、完工、查看、开工并报工、批量报工
- 弹窗：开工确认、完工报工、报工详情、批量报工

---

### 10.5 质量管理

#### 检验记录 `/quality/inspections`

页面类型：按生产批次分块展示

- 区块顶部：批次号、工单号、产品、检验状态
- 明细表格：检验数量、合格数量、不合格数量、检验结果、检验人员、检验时间
- 操作：新增、编辑、查看详情、上传检测文件、创建返工、确认合格入库
- 弹窗：新增/编辑检验记录、检验详情、上传检测文件、创建返工、确认合格入库

#### 返工记录 `/quality/reworks`

页面类型：按原生产批次分块展示

- 区块顶部：原生产批次号、工单号、产品、返工工序
- 明细表格：不合格数量、返工数量、返工原因、返工负责人、返工状态
- 操作：新增、编辑、分配返工负责人、填写返工结果、重新检验
- 弹窗：新增/编辑返工、分配返工负责人、填写返工结果、重新检验

---

## 11. 图标与视觉细节

### 11.1 图标

1. 使用线性图标。
2. 不使用复杂插画。
3. 关键操作可配图标，但不能喧宾夺主。
4. 状态图标可少量使用，如成功、警告、异常。

### 11.2 圆角与阴影

| 元素     |         圆角 | 阴影     |
| -------- | -----------: | -------- |
| 卡片     |          8px | 轻微或无 |
| 按钮     |          6px | 无       |
| 输入框   |          6px | 无       |
| 弹窗     |         10px | 中等阴影 |
| 状态标签 | 999px 或 4px | 无       |

### 11.3 空状态

空状态应包含简短提示、可选说明、主要操作按钮。

示例：

```text
暂无生产批次
创建工单并生成生产批次后，将在这里显示任务进度。
[新增工单]
```

---

## 12. 交互原则

1. 新增、编辑、分配、确认、上传均使用弹窗。
2. 不使用抽屉作为主要交互。
3. 删除、取消、关闭、作废必须二次确认。
4. 复杂流程按钮文案必须具体，例如“确认出库”“一键按默认派工”。
5. 表格操作后应给予消息提示。
6. 表单提交失败时，错误提示应定位到具体字段。
7. 跳转类操作必须明确，例如“跳转入库”。
8. 弹窗关闭时，如表单已修改，应提示是否放弃修改。

---

## 13. 响应式要求

一期管理端优先适配桌面端。

- 主要适配：1440px 及以上
- 最低支持：1280px
- 不优先适配手机端
- 表格在窄屏下允许横向滚动

---

## 14. 设计检查清单

页面交付前检查：

- 是否使用统一主色和状态色
- 是否使用弹窗而不是抽屉
- 表格字段是否按业务优先级排序
- 操作按钮是否过多
- 危险操作是否二次确认
- 表单是否有明确必填项
- 状态文案是否统一
- 空状态是否有说明和操作
- 页面是否符合管理端目录结构

---

## 15. 文档边界

本文件只描述 UI 设计规范。以下内容不放在本文件：

- 技术栈
- 代码目录结构
- AI 编码规则
- 数据库字段设计
- 接口命名规则
- 一期/二期开发边界
- 业务规则详细说明

这些内容应放入 `agents.md`、数据库设计文档或需求说明书中。
