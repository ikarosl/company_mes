# 系统管理

## 用户管理

```text
菜单路由
/system/users

菜单权限
system:users:view

------------------------------------------------

GET    /api/system/users
权限：system:users:view

GET    /api/system/users/{id}
权限：system:users:detail

POST   /api/system/users
权限：system:users:create

PUT    /api/system/users/{id}
权限：system:users:update

PUT    /api/system/users/{id}/enable
权限：system:users:enable

PUT    /api/system/users/{id}/disable
权限：system:users:disable

PUT    /api/system/users/{id}/reset-password
权限：system:users:reset-password

PUT    /api/system/users/{id}/roles
权限：system:users:assign-role
```

---

## 角色管理

```text
菜单路由
/system/roles

菜单权限
system:roles:view

------------------------------------------------

GET    /api/system/roles
权限：system:roles:view

GET    /api/system/roles/{id}
权限：system:roles:detail

POST   /api/system/roles
权限：system:roles:create

PUT    /api/system/roles/{id}
权限：system:roles:update

DELETE /api/system/roles/{id}
权限：system:roles:delete

PUT    /api/system/roles/{id}/enable
权限：system:roles:enable

PUT    /api/system/roles/{id}/disable
权限：system:roles:disable

PUT    /api/system/roles/{id}/permissions
权限：system:roles:assign-permissions
```

---

## 权限管理

```text
菜单路由
/system/permissions

菜单权限
system:permissions:view

------------------------------------------------

GET    /api/system/permissions
权限：system:permissions:view

GET    /api/system/permissions/tree
权限：system:permissions:view

GET    /api/system/permissions/{id}
权限：system:permissions:detail

POST   /api/system/permissions
权限：system:permissions:create

PUT    /api/system/permissions/{id}
权限：system:permissions:update

DELETE /api/system/permissions/{id}
权限：system:permissions:delete

PUT    /api/system/permissions/{id}/enable
权限：system:permissions:enable

PUT    /api/system/permissions/{id}/disable
权限：system:permissions:disable
```

---

## 日志管理

```text
菜单路由
/system/logs

菜单权限
system:logs:view

------------------------------------------------

GET /api/system/logs
权限：system:logs:view

GET /api/system/logs/{id}
权限：system:logs:detail

GET /api/system/logs?userId=&action=&startTime=&endTime=
权限：system:logs:view

GET /api/system/logs/export
权限：system:logs:export
```

---

# 产品管理

## 产品资料

```text
菜单路由
/product/products

菜单权限
product:products:view

------------------------------------------------

GET    /api/products
权限：product:products:view

GET    /api/products/{id}
权限：product:products:detail

POST   /api/products
权限：product:products:create

PUT    /api/products/{id}
权限：product:products:update

PUT    /api/products/{id}/enable
权限：product:products:enable

PUT    /api/products/{id}/disable
权限：product:products:disable

GET    /api/products/{id}/inventory
权限：product:products:view-inventory

GET    /api/products/{id}/routes
权限：product:products:view-route

PUT    /api/products/{id}/bom
权限：product:products:config-bom

PUT    /api/products/{id}/route
权限：product:products:bind-route
```

---

## 产品分类

```text
菜单路由
/product/categories

菜单权限
product:categories:view

------------------------------------------------

GET    /api/product-categories
权限：product:categories:view

GET    /api/product-categories/{id}
权限：product:categories:detail

POST   /api/product-categories
权限：product:categories:create

PUT    /api/product-categories/{id}
权限：product:categories:update

PUT    /api/product-categories/{id}/enable
权限：product:categories:enable

PUT    /api/product-categories/{id}/disable
权限：product:categories:disable

PUT    /api/product-categories/{id}/specs
权限：product:categories:config-spec
```

---

## 生产工序

```text
菜单路由
/product/processes

菜单权限
product:processes:view

------------------------------------------------

GET    /api/processes
权限：product:processes:view

GET    /api/processes/{id}
权限：product:processes:detail

POST   /api/processes
权限：product:processes:create

PUT    /api/processes/{id}
权限：product:processes:update

PUT    /api/processes/{id}/enable
权限：product:processes:enable

PUT    /api/processes/{id}/disable
权限：product:processes:disable

POST   /api/processes/{id}/sop
权限：product:processes:upload-sop
```

---

## 工艺路线

```text
菜单路由
/product/routes

菜单权限
product:routes:view

------------------------------------------------

GET    /api/routes
权限：product:routes:view

GET    /api/routes/{id}
权限：product:routes:detail

POST   /api/routes
权限：product:routes:create

PUT    /api/routes/{id}
权限：product:routes:update

DELETE /api/routes/{id}
权限：product:routes:delete

PUT    /api/routes/{id}/enable
权限：product:routes:enable

PUT    /api/routes/{id}/disable
权限：product:routes:disable

PUT    /api/routes/{id}/processes
权限：product:routes:config-processes
```

---

# 仓储管理

## 库存管理

```text
菜单路由
/warehouse/inventory

菜单权限
warehouse:inventory:view

------------------------------------------------

GET /api/warehouse/inventory
权限：warehouse:inventory:view

GET /api/warehouse/inventory/available
权限：warehouse:inventory:view-available

GET /api/warehouse/inventory/reserved
权限：warehouse:inventory:view-reserved

POST /api/warehouse/inventory/stocktake
权限：warehouse:inventory:stocktake

PUT /api/warehouse/inventory/adjust
权限：warehouse:inventory:adjust
```

---

## 出入库管理

```text
菜单路由
/warehouse/transactions

菜单权限
warehouse:transactions:view

------------------------------------------------

GET  /api/warehouse/transactions
权限：warehouse:transactions:view

POST /api/warehouse/transactions/inbound
权限：warehouse:transactions:inbound

POST /api/warehouse/transactions/outbound
权限：warehouse:transactions:outbound

POST /api/warehouse/transactions/shipment
权限：warehouse:transactions:shipment

POST /api/warehouse/transactions/return
权限：warehouse:transactions:return

GET  /api/warehouse/transactions/{id}
权限：warehouse:transactions:detail
```

---

# 生产管理

## 工单管理

```text
菜单路由
/production/orders

菜单权限
production:orders:view

------------------------------------------------

GET    /api/orders
权限：production:orders:view

GET    /api/orders/{id}
权限：production:orders:detail

POST   /api/orders
权限：production:orders:create

PUT    /api/orders/{id}
权限：production:orders:update

PUT    /api/orders/{id}/draft
权限：production:orders:draft

PUT    /api/orders/{id}/release
权限：production:orders:release

PUT    /api/orders/{id}/close
权限：production:orders:close

PUT    /api/orders/{id}/cancel
权限：production:orders:cancel
```

---

## 工单任务分配

```text
GET  /api/orders/{id}/tasks
权限：production:orders:tasks:view

POST /api/orders/{id}/tasks
权限：production:orders:tasks:create

PUT  /api/orders/{id}/tasks/{taskId}
权限：production:orders:tasks:update

POST /api/orders/{id}/material-demand
权限：production:orders:generate-material-demand

POST /api/orders/{id}/material-allocation
权限：production:orders:allocate-material
```

---

## 任务管理

```text
菜单路由
/production/tasks

菜单权限
production:tasks:view

------------------------------------------------

GET    /api/tasks
权限：production:tasks:view

GET    /api/tasks/{id}
权限：production:tasks:detail

POST   /api/tasks
权限：production:tasks:create

PUT    /api/tasks/{id}
权限：production:tasks:update

POST   /api/tasks/{id}/material-demand
权限：production:tasks:generate-material-demand

POST   /api/tasks/{id}/material-allocation
权限：production:tasks:allocate-material

POST   /api/tasks/{id}/dispatch
权限：production:tasks:dispatch

PUT    /api/tasks/{id}/start
权限：production:tasks:start

PUT    /api/tasks/{id}/finish
权限：production:tasks:finish

POST   /api/tasks/{id}/rework
权限：production:tasks:create-rework

GET    /api/tasks/{id}/trace
权限：production:tasks:view-trace
```

---

## 物料分配

```text
菜单路由
/production/material-allocation

菜单权限
production:material-allocation:view

------------------------------------------------

GET  /api/material-allocation
权限：production:material-allocation:view

POST /api/material-allocation/generate-demand
权限：production:material-allocation:generate-demand

POST /api/material-allocation/allocate
权限：production:material-allocation:allocate

PUT  /api/material-allocation/{id}/confirm-kit
权限：production:material-allocation:confirm-kit

PUT  /api/material-allocation/{id}/confirm-outbound
权限：production:material-allocation:confirm-outbound

POST /api/material-allocation/{id}/return
权限：production:material-allocation:return-material
```

---

# 质量管理

## 检验记录

```text
菜单路由
/quality/inspections

菜单权限
quality:inspections:view

------------------------------------------------

GET    /api/quality/inspections
权限：quality:inspections:view

GET    /api/quality/inspections/{id}
权限：quality:inspections:detail

POST   /api/quality/inspections
权限：quality:inspections:create

PUT    /api/quality/inspections/{id}
权限：quality:inspections:update

POST   /api/quality/inspections/{id}/files
权限：quality:inspections:upload-file

POST   /api/quality/inspections/{id}/rework
权限：quality:inspections:create-rework

PUT    /api/quality/inspections/{id}/confirm-inbound
权限：quality:inspections:confirm-inbound
```

---

## 返工记录

```text
菜单路由
/quality/reworks

菜单权限
quality:reworks:view

------------------------------------------------

GET    /api/quality/reworks
权限：quality:reworks:view

GET    /api/quality/reworks/{id}
权限：quality:reworks:detail

POST   /api/quality/reworks
权限：quality:reworks:create

PUT    /api/quality/reworks/{id}
权限：quality:reworks:update

PUT    /api/quality/reworks/{id}/owner
权限：quality:reworks:assign-owner

PUT    /api/quality/reworks/{id}/result
权限：quality:reworks:submit-result

POST   /api/quality/reworks/{id}/reinspect
权限：quality:reworks:reinspect
```

---

# 员工端

## 我的任务

```text
菜单路由
/worker/tasks

菜单权限
worker:tasks:view

------------------------------------------------

GET    /api/worker/tasks
权限：worker:tasks:view

GET    /api/worker/tasks/{id}
权限：worker:tasks:detail

GET    /api/worker/tasks/{id}/sop
权限：worker:tasks:view-sop

PUT    /api/worker/tasks/{id}/start
权限：worker:tasks:start

PUT    /api/worker/tasks/{id}/complete
权限：worker:tasks:complete

GET    /api/worker/tasks/{id}/history
权限：worker:tasks:history
```

---

# 检测端

## 检测任务

```text
菜单路由
/inspector/tasks

菜单权限
inspector:tasks:view

------------------------------------------------

GET    /api/inspector/tasks
权限：inspector:tasks:view

GET    /api/inspector/tasks/{id}
权限：inspector:tasks:detail

GET    /api/inspector/tasks/{id}/spec
权限：inspector:tasks:view-spec

PUT    /api/inspector/tasks/{id}/result
权限：inspector:tasks:submit-result

POST   /api/inspector/tasks/{id}/files
权限：inspector:tasks:upload-file

POST   /api/inspector/tasks/{id}/rework
权限：inspector:tasks:create-rework
```
