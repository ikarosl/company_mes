# 统一库存方案迁移清单

本文档用于指导本分支从旧仓储物料方案迁移到 `docs/newSqlDesign.md` 中的统一库存对象方案。迁移目标不是一次性推翻全部业务代码，而是按可验证的小步逐步完成，确保前后端权限、路由、接口契约、数据库和页面展示始终保持一致。

## 一、迁移原则

1. 方案二以 `docs/newSqlDesign.md` 为数据库设计基准。
2. 旧 `material_batches`、`batch_material_usages`、`material_batch`、`material_demand`、`material_allocation` 不再作为后续开发依据。
3. 库存对象统一使用 `item_info`，库存批次统一使用 `item_batch`，库存数量统一来自 `inventory_transaction` 和视图汇总。
4. 生产批次 `production_batches` 与库存批次 `item_batch` 必须分离，不允许把生产批次 ID 当库存批次 ID 使用。
5. 分配只表达预留占用，不生成库存流水；入库、出库、退料、报废、盘点调整必须生成库存流水。
6. 前后端共享类型、接口路径、权限编码必须先在 `packages/api-contract` 和 `packages/constants` 中统一，再进入业务实现。
7. 从主分支复用前端代码时，只按功能点 diff 后提取可复用 UI、组件、表格交互、弹窗结构和消息反馈，不直接整块合并旧仓储数据流。

## 二、已完成事项

- [x] 整合 `docs/仓库表新方案.md` 到 `docs/newSqlDesign.md`
- [x] 新增统一库存数据库迁移脚本：`docs/20260629_apply_unified_inventory.sql`
- [x] 新增统一库存权限迁移脚本：`docs/20260629_unified_inventory_permissions.sql`
- [x] 同步仓储权限常量：`packages/constants/src/permissions.ts`
- [x] 同步仓储接口路径常量：`packages/api-contract/src/routes.ts`
- [x] 新增仓储共享类型：`packages/api-contract/src/warehouse.ts`
- [x] 同步管理端仓储菜单与占位路由：`apps/admin-web/src/router/routes.ts`、`apps/admin-web/src/views/AdminLayout.vue`
- [x] 同步接口权限文档：`docs/接口路由+权限编码.md`
- [x] 保留 `warehouse:inventory:stocktake` 作为旧接口过渡兼容权限，后续库存重构完成后再清理

## 三、阶段一：数据库基线迁移

- [ ] 评审 `docs/20260629_apply_unified_inventory.sql` 中所有外键是否与当前 `company_mes_v2` 现有表字段一致
- [ ] 确认 `work_orders.id`、`production_batches.id`、`users.id` 类型与新脚本中的 `BIGINT UNSIGNED` 一致
- [ ] 在本地 `company_mes_v2` 执行统一库存建表脚本
- [ ] 执行 `docs/20260629_unified_inventory_permissions.sql` 补齐权限
- [ ] 验证新表、视图、权限数量
- [ ] 验证中文枚举和备注无乱码
- [ ] 将执行方式记录到迁移说明，SQL 文件必须使用 `SOURCE` 和 `--default-character-set=utf8mb4`

## 四、阶段二：后端仓储模块骨架

- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/items`
- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/inventory`
- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/inbound-orders`
- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/outbound-orders`
- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/return-orders`
- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/scraps`
- [ ] 新增或调整后端目录为 `apps/backend/src/warehouse/stock-checks`
- [ ] 所有 Controller 使用 `@RequirePermission(PERMISSIONS.warehouse.xxx)`
- [ ] 所有 DTO、Service、Repository 关键字段和关键逻辑补充业务注释
- [ ] 写操作确认走统一响应和审计日志

## 五、阶段三：库存对象与库存批次

- [ ] 实现库存对象 `item_info` 列表、详情、新增、编辑、启用、停用
- [ ] 实现库存类型 `item_type` 查询或选项接口
- [ ] 实现库存批次现存量列表，数据来源为 `v_item_batch_stock`
- [ ] 实现可分配库存列表，数据来源为 `v_item_batch_available_to_allocate`
- [ ] 替换旧 `material_batches.quantity` 查询逻辑
- [ ] 替换旧 `batch_material_usages` 预留汇总逻辑
- [ ] 移除页面和接口中对旧批次状态 `available/partial_used/used_up/disabled` 的业务依赖

## 六、阶段四：入库、出库、退料、报废、盘点

- [ ] 实现入库单 `inbound_order` + `inbound_detail`
- [ ] 确认入库时生成 `inventory_transaction`
- [ ] 实现生产领料出库 `outbound_order` + `outbound_detail`
- [ ] 确认出库时按 `outbound_detail.id` 生成负数库存流水
- [ ] 实现退料单 `return_order` + `return_detail`
- [ ] 退料入库时按 `release_after_return` 处理预留释放语义
- [ ] 实现报废单 `item_scrap`
- [ ] 报废确认时按场景生成库存流水或补料需求
- [ ] 实现盘点单 `stock_check_order` + `stock_check_detail`
- [ ] 盘点调整只生成库存流水，不直接改批次数量
- [ ] 所有库存流水写入 `idempotency_key`，防止重复执行

## 七、阶段五：生产物料需求与分配

- [ ] 将旧 `product_materials` / `route_step_materials` 用料生成逻辑映射到 `product_bom`
- [ ] 实现 `production_item_demand` 需求生成
- [ ] 实现 `production_item_allocation` 分配
- [ ] 分配时查询 `v_item_batch_available_to_allocate`
- [ ] 出库时校验分配行可出库数量
- [ ] 报废补料新增需求，不修改原需求 `need_number`
- [ ] 生产批次详情改为读取 `v_production_item_demand_summary`
- [ ] 生产批次投入汇总改为读取 `v_production_batch_item_summary`
- [ ] 生产产出汇总改为读取 `v_production_batch_output_summary`

## 八、阶段六：前端迁移

- [ ] 新增或迁移 `apps/admin-web/src/views/warehouse/ItemsPage.vue`
- [ ] 重构 `WarehouseInventoryPage.vue` 为库存批次与现存量页面
- [ ] 新增入库、出库、退料、报废、盘点页面
- [ ] 新增或调整 `apps/admin-web/src/api/warehouse.ts` 中的接口封装
- [ ] 所有页面使用 `packages/api-contract/src/warehouse.ts` 的共享类型
- [ ] 所有页面操作按钮使用 `PERMISSIONS.warehouse.xxx` 控制
- [ ] 新增、编辑、确认、取消、报废、盘点调整等操作必须使用 `EMessage` 成功/失败提示
- [ ] 高风险操作必须二次确认
- [ ] 保持管理端表格优先、弹窗交互，不使用抽屉作为主要交互

## 九、从主分支复用前端代码规则

推荐方式：按需 diff，不建议直接合并前端代码。

原因：

1. 主分支已经沿旧仓储方案超前开发，直接合并会把旧表名、旧状态、旧接口路径和旧权限码带回方案二。
2. 方案二的数据模型变化较大，旧页面的数据字段和状态计算多数需要重接。
3. 前端可复用价值主要在表格布局、筛选区、弹窗表单、状态标签、分页、消息提示和权限按钮，不在旧接口数据流本身。

执行方式：

- [ ] 每迁移一个页面前，先对比 `main` 与当前分支对应页面
- [ ] 提取可复用组件结构、表单布局、样式、交互反馈
- [ ] 丢弃旧接口路径、旧 DTO、旧状态枚举、旧库存计算
- [ ] 复用前先确认权限码来自 `packages/constants/src/permissions.ts`
- [ ] 复用前先确认接口路径来自 `packages/api-contract/src/routes.ts`
- [ ] 复用后运行类型检查和乱码检查

常用 diff 命令示例：

```powershell
git diff main...HEAD -- apps/admin-web/src/views/warehouse
git show main:apps/admin-web/src/views/warehouse/WarehouseInventoryPage.vue
git show main:apps/admin-web/src/api/warehouse.ts
```

## 十、阶段七：清理旧方案残留

- [ ] 清理后端 `material_batches` 查询
- [ ] 清理后端 `batch_material_usages` 查询
- [ ] 清理前端旧 `MaterialBatch*` 类型依赖
- [ ] 清理 `warehouse:inventory:stocktake` 过渡权限
- [ ] 清理旧仓储 SQL 文档或标记为方案一历史资料
- [ ] 清理旧菜单、旧路由、旧接口文档残留
- [ ] 更新 `docs/init_rbac.sql` 或最终初始化脚本中的权限种子
- [ ] 更新联调说明和数据库初始化说明

## 十一、每阶段交付检查

- [ ] 改了哪些文件已说明
- [ ] 影响哪些模块已说明
- [ ] 数据库变更和迁移脚本已说明
- [ ] 权限、路由、常量、接口契约一致
- [ ] 前后端共享类型放在 `packages/api-contract`
- [ ] 后端权限装饰器不手写权限字符串
- [ ] 前端菜单和路由 `meta.permission` 一致
- [ ] 关键业务代码有必要注释
- [ ] 前端成功/失败提示完整
- [ ] 中文编码和乱码风险已检查
